# struct-prog-lang-hw-3

## Inside the prog lang folder is every file needed to run the updated language with a watch variable 

To run with a watch variable and using the test file i provided, enter this into terminal:

python3 runner.py myfile.txt watch=x
